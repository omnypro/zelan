import { AdapterManager } from './AdapterManager'

/**
 * Singleton instance of the adapter manager
 */
export const adapterManager = new AdapterManager()

/**
 * Export the AdapterManager type
 */
export type { AdapterManager }
